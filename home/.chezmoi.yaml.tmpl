{{- $useNvim := promptBool "neovim" true -}}
data:
  useNvim: {{ $useNvim }}
merge:
  command: nvim
  args:
    - "-d"
    - "{{"{{"}} .Destination {{"}}"}}"
    - "{{"{{"}} .Source {{"}}"}}"
    - "{{"{{"}} .Target {{"}}"}}"
