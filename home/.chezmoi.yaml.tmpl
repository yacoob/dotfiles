umask: 0o027
merge:
  command: nvim
  args:
    - "-d"
    - "{{"{{"}} .Destination {{"}}"}}"
    - "{{"{{"}} .Source {{"}}"}}"
    - "{{"{{"}} .Target {{"}}"}}"
data:
  chassisType: {{ (output "hostnamectl" "--json=short" | mustFromJson).Chassis }}
