{{- $devEnv := promptBool "dev-environment" false -}}
data:
  devEnv: {{ $devEnv }}
merge:
  command: nvim
  args:
    - "-d"
    - "{{"{{"}} .Destination {{"}}"}}"
    - "{{"{{"}} .Source {{"}}"}}"
    - "{{"{{"}} .Target {{"}}"}}"
