[color]
	ui = true
[core]
	excludesfile = ~/.cvsignore
[lastfm]
	user = boocay
	maxhours = 4
	groupby = album
[push]
	default = simple
[include]
# wildcards don't work, but nonexisting files are ignored :|
	path = .gitconfig.osx
  path = .gitconfig.linux
  path = .gitconfig.home
  path = .gitconfig.office
